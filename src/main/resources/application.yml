---

spring:
  profiles:
    active: local
    
server:
  port: 8081
  

---

spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:/native-config
         